#include<iostream>
#include<vector>
#include<pair>
#include<string>
using namespace std;

const string whitespaces (" \t\f\v\n\r");
vector<pair<string, int>> historia;
long wiersz = 1;

void usun_biale(string s) {
	int found = str.find_last_not_of(whitespaces);
	if (found!=std::string::npos)
		str.erase(found+1);
	else
		str.clear();            // str is all whitespace
}

bool isDigit(char c) {
	
}
bool sprawdz(const string s) {
	int i = s.length-1;
	bool cyfra = bialy = minus = false;
	while (i>=0 && (cyfra || bialy || minus)) {
		cyfra = isdigit(s[i]);
		
	}
}

pair<string, int> stworzMonete(string s);

int main() {
	pair<string, int> moneta;
	string s;
	while(scanf("%s", &s)) {
	//obczajamy czy string jest okej
		if (sprawdz(s)) {
			moneta = stworzMonete(s);
		} else {
			cerr << "Error in line: " + wiersz;
		}
		wiersz++;
	}
	
}

